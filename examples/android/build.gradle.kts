// Root build for the Android quickstart. Plugin versions are pinned here and
// applied in :app. They match the versions the SDK build uses.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
