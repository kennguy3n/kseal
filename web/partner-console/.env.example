# Base URL of the kseal server. Vite inlines this at build time.
# In production a prebuilt image can override it at deploy time via the
# KSEAL_API_BASE_URL container env (see README "Runtime configuration").
VITE_KSEAL_API_BASE_URL=http://localhost:8080
